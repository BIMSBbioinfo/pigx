#!@GNUBASH@

set -e
set -u

chmod +w ${srcdir}/Tests

${builddir}/pigx-chipseq -s ${srcdir}/Tests/settings.yaml ${srcdir}/Tests/sample_sheet.yaml

if $?
then
  echo "ERROR: the pipeline failed with an unspecified error"
  exit 1
fi
